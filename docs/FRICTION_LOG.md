# Friction Log

This file is intentionally evidence-driven. Add an entry only after reproducing the issue while using an Amazon developer tool, API or SDK.

## Entry template

### FL-XXX — Short title

- **Date:**
- **Tool/API/SDK:**
- **Task attempted:**
- **Steps taken:**
- **Expected:**
- **Actual:**
- **Severity:** low / medium / high / blocking
- **Workaround:**
- **Actionable suggestion:**
- **Evidence:** command output, screenshot, request ID, or documentation URL

---

## Build-environment observation — not an Amazon friction entry

The isolated environment used to assemble the initial repository could not resolve PyPI, so the MCP package could not be installed there. The HomeOps domain engine and web/API flow were still executed and tested using preinstalled dependencies.

This is **not** an Amazon product issue and should not be submitted as hackathon friction feedback.
